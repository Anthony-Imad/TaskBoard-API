# TaskBoard-API
a RESTful API for a task/project management system with users, projects and tasks. we will implement endpoints validation, authentication, reporting, and testing.
-=keep in mind that this will be used as a guide to learn
